{
  "version": 2,
  "builds": [
    {
      "src": "vercel.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/vercel.js" }
  ],
  "buildCommand": "cd frontend && npm install && npm run build && ls -la dist",
  "env": {
    "NODE_ENV": "production"
  }
} 